
use super::Token;

#[derive(Debug)]
pub struct Parser {

}

impl Parser {
    pub fn new() -> Parser {
        Parser {

        }
    }

    pub fn parse(&mut self, tokens: Vec<Token>) -> () {
        ()
    }
}